import Button from "@mui/material/Button";
import Box from "@mui/material/Box";

const GenerateCars = () => {
    return (
        <Box>
            <Button variant="outlined" sx={{ mr: 2, fontSize: "1rem" }}>Generate Cars</Button>
        </Box>
    );
};

export default GenerateCars;
